version: "3.8"

services:
  messaging:
    build: ./backend
    command: "sleep infinity"
    volumes: 
      - ./:/workspace
    
  portal:
    build: ./frontend
    command: "sleep infinity"
    volumes:
      - ./:/workspace
      - /workspace/frontend/node_modules
    ports:
      - "4200:4200"